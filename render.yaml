services:
  - type: web
    name: battery-cost-estimator
    env: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn app:server
    autoDeploy: true
